<h1>Your Albums</h1>
<ul>
		<%= render @albums %>
</ul>

<%= form_for(current_user.albums.build) do |f| %>
	<div class="field">
		<%= f.text_field :title, placeholder: "Album Title" %>
	</div>
		<%= f.submit "Add Album", class: "btn btn-primary" %>
<% end %>
